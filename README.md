# Tyler Maynard's Personal App

## Installation

+ clone rep `git clone https://github.com/TylersDesk/tdesk.com.git`

+ `cd tdesk.com`

+ run `npm install`

+ run `node scripts/web-server.js`

+ navigate to `http://localhost:8000`